import { Navigation } from '../Navigation/Navigation';

export const AppBar = () => {
  // const { isLoggedIn } = useAuth();

  return (
    <>
      <Navigation />
      {/* {isLoggedIn && <UserMenu />} */}
      {/* {isLoggedIn && <Auth />} */}
    </>
  );
};
